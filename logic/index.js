'use strict';

/**
 *
 * @param {string} text - the text to search
 * @param {string} toReplace - the thing to replace
 * @param {string} withThis - what to replace it with
 * @returns {string} the text with the thing replaced with the other thing
 */
const replace = (text, toReplace, withThis) => {
  return 'write me!';
};

module.exports = replace;
